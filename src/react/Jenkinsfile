

// pipeline {
//     agent { 
//         dockerfile {
//             filename 'Dockerfile'
//             dir "src/react"
//         }
//     }
//     stages {
//         stage ('Test') {
//             steps {
//                 echo 'hola mundo'
//             }
//         }
//     }
// }

pipeline {
    agent any
    environment {
        _REPOSITORY = "liverpool-portal-uniformes-dev-images-apps"
        _IMAGE = "frontend-uniformes-service"
        _LOCATION = "us-east4"
        _DESTINATION_PROJECT_ID = "labuniformes"
        _IMAGE_PATH = "${_LOCATION}-docker.pkg.dev/${_DESTINATION_PROJECT_ID}/${_REPOSITORY}/${_IMAGE}"
    }
    stages {
        stage ('Test') {
            steps {
                dir('src/react') {
                    withCredentials([file(credentialsId: 'key-sa', variable: 'GC_KEY')]) {
                        sh("gcloud auth activate-service-account --key-file=${GC_KEY}")
                        sh("gcloud auth configure-docker ${_LOCATION}-docker.pkg.dev")
                        sh("docker build -t ${_IMAGE_PATH} .")
                        sh("docker push ${_IMAGE_PATH}")
                    }
                }
            }
        }
    }
}
